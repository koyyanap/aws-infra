Resources:
  MyRepository: 
    Type: AWS::ECR::Repository
    Properties: 
      RepositoryName: "test-repository"

  MyRepository1: 
    Type: AWS::ECR::Repository
    Properties: 
      RepositoryName: "test-repository-1"
    